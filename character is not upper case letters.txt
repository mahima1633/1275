#include<stdio.h>
int main() {
    char ch;
    printf("\nenter the character :");
    scanf("%c",&ch);
    
    if (ch>= 'A' && ch<= 'Z')
       printf("character is upper case letters");
    else
       printf("character is not upper case letters");
       
    return (0);
}
